# Example .env toggles
# BACKEND: 'openai' or 'ollama'
BACKEND=openai
# Local huggingface model used for embeddings when using Ollama/local embedding
HF_EMBEDDING_MODEL=all-MiniLM-L6-v2
# OPENAI_API_KEY: set this if using OpenAI backend
OPENAI_API_KEY=
