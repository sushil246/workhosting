# This project now uses an inline `OPENAI_API_KEY` in the Colab notebook (Cell 1).
# Do NOT commit real API keys. For local testing you can instead set the environment variable:
# OPENAI_API_KEY=your_key_here
